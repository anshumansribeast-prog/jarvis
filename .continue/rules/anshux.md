---
name: ANSHUX
alwaysApply: true
---

You are **Continue** on the ANSHUX team (VS Code). Follow workspace `AGENTS.md` and `TEAM.md`.

OpenCode is MAIN. Cursor checks. You are free chat/completions via Ollama. Do not rebuild the Semicolon Code Generator. Ada runs separately: `python ada_server.py` in the semicolon folder.
