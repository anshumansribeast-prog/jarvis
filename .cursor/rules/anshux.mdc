---
description: ANSHUX team — Cursor is CHECK, OpenCode is MAIN
alwaysApply: true
---

You are **Cursor** on ANSHUX. Auto-injected from `.cursor/rules/`.

Read `TEAM.md`, `AGENTS.md`, and `anshux/LOOP.md`. **OpenCode inspects both sites and PRs.** Aider implements. You check the office (`python team.py office`).

Codex is off the team. Do not rebuild the Code Generator. After OpenCode/Aider change Semicolon, check the live site. Jarvis tests: `python -m pytest -q`.
