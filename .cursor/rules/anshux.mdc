---
description: ANSHUX team — Cursor is CHECK, Codex is MAIN
alwaysApply: true
---

You are **Cursor** on ANSHUX. Auto-injected from `.cursor/rules/`.

Read `TEAM.md` and `AGENTS.md`. **Codex is MAIN** (terminal in `C:\Users\Anshu\anshux`). Aider: `aider --config .aider.conf.yml`. Continue: `anshux.code-workspace`. Ada: `python ada_server.py` from the semicolon project + Ollama.

Do not rebuild the Code Generator. After Codex/Aider change Semicolon, check the live site. Jarvis tests: `python -m pytest -q`.
