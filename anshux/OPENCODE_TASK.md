Paste into **OpenCode** (MAIN) after `cd C:\Users\Anshu\anshux` then `opencode`:

You are MAIN on ANSHUX with Cursor (check) and free backups Aider/Continue + Ollama. Codex is off the team. Do not idle.

Semicolon PR https://github.com/anshumansribeast-prog/semicolon/pull/8 is OPEN. Generator removal is correct. That PR deleted too much:

RESTORE on that branch (do not bring Generator back):
- js/ada-api.js and Ada POST /api/ada (chat must work)
- ada_knowledge.py if Ada still uses it
- pages/concepts.html and Concepts in the nav
- img/ada.jpg

Keep deleted: pages/generate.html, js/generate.js, Practice #tabGen/#modeGen.

Then push the PR. Cursor will check the live site after deploy.
