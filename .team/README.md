# How each teammate starts

Clone or use this repo as `C:\Users\Anshu\anshux`.

```powershell
cd C:\Users\Anshu\anshux
```

| Agent | Command |
| --- | --- |
| **OpenCode** | `opencode` — reads `AGENTS.md` + `TEAM.md` (`opencode.json`). Install: `anshux/OPENCODE_START.md`. |
| **Aider** | `aider --config .aider.conf.yml` |
| **Cursor** | Open this folder (rules in `.cursor/rules/*.mdc`). |
| **Continue** | Open `anshux.code-workspace` (reads `.continue/rules/` + `AGENTS.md`). |
| **Ada** | `cd C:\Users\Anshu\semicolon` then `python ada_server.py` (Ollama on). If semicolon is `projects\semicolon` under anshux, `cd` there instead. |

Codex is not on the team.
